apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: jenkins
spec:
  type: NodePort
  selector:
    app: jenkins
  ports:
    - protocol: TCP
      port: {{ .Values.jenkins.servicePort }}
      targetPort: {{ .Values.jenkins.servicePort }}
      nodePort: 32000
