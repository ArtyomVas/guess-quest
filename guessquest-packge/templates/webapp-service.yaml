apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: default
spec:
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: {{ .Values.webapp.servicePort }}
      targetPort: {{ .Values.webapp.containerPort }}
